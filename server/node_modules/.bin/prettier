#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@strapi/strapi/node_modules/prettier/bin/prettier.cjs" "$@"
  ret=$?
else 
  node  "$basedir/../@strapi/strapi/node_modules/prettier/bin/prettier.cjs" "$@"
  ret=$?
fi
exit $ret
